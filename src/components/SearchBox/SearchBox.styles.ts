import styled from "@emotion/styled";

const SearchBoxStyles = styled("div")(() => ({
  padding: "0px 2px",
  position: "relative",
  pointerEvents: "auto",
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  borderRadius: 1,
  backgroundColor: "#efefef",
  margin: "1rem",
  maxWidth: "20%",
  boxShadow:
    " 0px 2px 1px -1px #dadadf, 0px 1px 1px 0px rgb(241 227 227 / 14%), 5px 8px 6px 0px rgb(200 209 207 / 12%)}))",
}));

export default SearchBoxStyles;
